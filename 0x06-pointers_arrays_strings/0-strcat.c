#include <string.h>

/**
 * _strcat -  concatenates two strings.
 * @dest: string to be passed
 * @src: string to be appended to dest
 *
 * Return: concatenated string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
